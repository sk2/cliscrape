---
phase: 02-legacy-compatibility-cli
plan: 04
type: execute
wave: 2
depends_on: ["02-03"]
files_modified:
  - src/lib.rs
  - src/engine/macros.rs
  - src/engine/fsm.rs
  - src/template/loader.rs
  - src/template/textfsm.pest
  - tests/textfsm_compat.rs
  - tests/fixtures/textfsm/explicit_eof_empty.textfsm
  - tests/fixtures/textfsm/explicit_eof_rules.textfsm
  - tests/fixtures/textfsm/warn_skip_constructs.textfsm
autonomous: true

must_haves:
  truths:
    - "Undefined patterns are errors (unknown ${VALUE} placeholders and unknown {{macros}} do not silently pass)"
    - "Explicit `EOF` state behavior matches TextFSM (empty EOF suppresses implicit record; EOF rules run when present)"
    - "Unsupported/unknown constructs warn and are skipped where feasible (without crashing template load)"
    - "Warnings are surfaced via a library API; the library does not print to stderr"
  artifacts:
    - path: "src/lib.rs"
      provides: "Template load entrypoint returning (parser, warnings) for CLI"
    - path: "src/template/loader.rs"
      provides: "Warn+skip TextFSM loader behavior for selected constructs"
    - path: "tests/textfsm_compat.rs"
      provides: "Regression coverage for strict validation + EOF + warnings"
  key_links:
    - from: "src/lib.rs"
      to: "src/main.rs"
      via: "CLI prints template-load warnings to stderr (plan 02-02)"
---

<objective>
Finish Phase-2 TextFSM compatibility edge behavior: strict undefined placeholder/macro validation, reserved `EOF` state semantics, and a non-printing warning channel enabling warn+skip handling of selected unsupported constructs.

Purpose: Keep TextFSM templates safe and predictable: undefined tokens are hard errors; EOF is TextFSM-compatible; and skippable unsupported bits produce structured warnings that the CLI can report.
Output: Loader/runtime validation updates, new warning-returning load API, and fixture-backed tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-legacy-compatibility-cli/02-CONTEXT.md
@.planning/phases/02-legacy-compatibility-cli/02-RESEARCH.md
@src/template/loader.rs
@src/engine/fsm.rs
@src/engine/macros.rs
@src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce strict undefined placeholder + macro validation</name>
  <files>src/engine/macros.rs, src/engine/fsm.rs, tests/textfsm_compat.rs</files>
  <action>
    1. Unknown `{{macro}}` is an error:
       - In `src/engine/macros.rs`, change macro expansion so unknown `{{name}}` macros return a template-load error (do not preserve verbatim).
    2. Unknown `${VALUE}` placeholder is an error:
       - In `src/engine/fsm.rs` during `Template::from_ir` compilation, after macro expansion + value placeholder replacement, detect any remaining `${...}` tokens and fail template load with a clear error.
       - Also detect any remaining `{{...}}` tokens and fail (should now be redundant, but keeps the invariant explicit).
    3. Tests:
       - In `tests/textfsm_compat.rs`, add two tests that attempt to load templates containing an unknown `${MISSING}` and an unknown `{{missing_macro}}` and assert they error.
  </action>
  <verify>`cargo test -q` passes and the new strict-validation tests fail prior to this change.</verify>
  <done>Undefined placeholders/macros are hard errors with clear messages; no silent verbatim tokens remain in compiled regexes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reserved `EOF` state semantics</name>
  <files>src/engine/fsm.rs, src/template/textfsm.pest, tests/textfsm_compat.rs, tests/fixtures/textfsm/explicit_eof_empty.textfsm, tests/fixtures/textfsm/explicit_eof_rules.textfsm</files>
  <action>
    Implement TextFSM reserved-state behavior for `EOF`:
    1. If the template defines an explicit `EOF` state with zero rules: suppress the implicit EOF record emission.
    2. If the template defines an explicit `EOF` state with rules: execute those rules once at end-of-input (treat EOF as a pseudo-line, typically empty string) and apply any record actions/state transitions.
    3. If the template does not define `EOF`: keep the current implicit-record behavior.
    4. Fixtures (create/ensure):
       - Ensure `tests/fixtures/textfsm/explicit_eof_empty.textfsm` and `tests/fixtures/textfsm/explicit_eof_rules.textfsm` exist and are used by tests.
    5. Parser grammar:
       - Adjust `src/template/textfsm.pest` only if needed so a state block named `EOF` parses correctly.
    6. Tests:
       - In `tests/textfsm_compat.rs`, add/adjust tests to assert both fixtures behave as described (empty EOF suppresses implicit record; EOF rules execute once).
  </action>
  <verify>`cargo test -q` passes and EOF behavior is covered by the two explicit EOF fixtures.</verify>
  <done>Explicit `EOF` behavior matches TextFSM semantics and is fixture-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Add warning-returning load API + warn/skip support for selected constructs</name>
  <files>src/lib.rs, src/template/loader.rs, tests/textfsm_compat.rs, tests/fixtures/textfsm/warn_skip_constructs.textfsm</files>
  <action>
    1. Warning channel (library-only; no printing):
       - Define a lightweight warning type in the library (e.g., `pub struct ScrapeWarning { kind: String, message: String }`).
       - Add a new API in `src/lib.rs` (or a `cliscrape::template` module) that returns warnings:
         - `load_template_with_warnings(path: &Path, format: TemplateFormat) -> Result<(FsmParser, Vec<ScrapeWarning>), ScraperError>`
       - Keep existing `FsmParser::{from_file,from_file_with_format}` behavior unchanged.

    2. Warn+skip behavior in `src/template/loader.rs` for a small concrete set:
       - Unknown `Value` option flags in the comma list:
         - Example: `Value Required,Filldown,Bogus (\S+)`.
         - Warn `unknown_value_flag` and ignore only the unknown flag.
       - Unknown rule action keyword:
         - Example: `^foo -> Frobnicate`.
         - Warn `unknown_rule_action` and skip that *rule line*.
       - Unsupported compound action token where a safe skip is possible:
         - Example: `-> Continue.Record` if not implemented.
         - Warn `unsupported_compound_action` and skip that *rule line*.
       - Keep strict failures for anything that makes semantics unsafe/ambiguous (undefined patterns, disallowed Continue+state transition, etc.).

    3. Fixture + tests:
       - Ensure `tests/fixtures/textfsm/warn_skip_constructs.textfsm` exists and includes at least:
         - a `Value` line with one unknown flag (should warn but still load);
         - a rule with an unknown action (should warn+skip);
         - enough valid rules so parsing a simple input string still produces at least one row.
       - In `tests/textfsm_compat.rs`, add a test that:
         - Calls `load_template_with_warnings(...)` and asserts `Ok((parser, warnings))` with `warnings.len() >= 2` and expected `kind` values.
         - Runs the parser over an input that exercises only the valid rules and asserts rows are produced.
       - Ensure comment lines are ignored even with leading whitespace (`^\s*#`) by adding such comments to this fixture (or another used fixture) and asserting it still loads/parses.
  </action>
  <verify>`cargo test -q` passes and warnings are observable only via the new API (no stderr writes from library code).</verify>
  <done>Template loads can return structured warnings for selected skippable constructs; CLI can surface these warnings without library-side printing.</done>
</task>

</tasks>

<verification>
1. `cargo test -q` passes.
2. Compatibility tests cover: explicit EOF empty vs EOF rules, undefined macro/placeholder errors, and warning-returning loader behavior.
</verification>

<success_criteria>
- Undefined placeholders/macros fail template load deterministically.
- `EOF` reserved-state behavior matches TextFSM.
- Unsupported-but-skippable constructs yield warnings and do not crash template load.
</success_criteria>

<output>
After completion, create `.planning/phases/02-legacy-compatibility-cli/02-04-SUMMARY.md`
</output>

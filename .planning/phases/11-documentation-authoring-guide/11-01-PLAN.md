---
phase: 11-documentation-authoring-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/usage.md
  - docs/_fixtures/usage/inputs/hostname.txt
  - docs/_fixtures/usage/templates/hostname.yaml
autonomous: true

must_haves:
  truths:
    - "User can discover how to use cliscrape from README and reach deeper docs in docs/"
    - "User can run parse with a local template and file input using copy-paste examples"
    - "User can learn how to list and inspect embedded templates (list-templates/show-template)"
  artifacts:
    - path: "README.md"
      provides: "Short entry point + links to docs + minimal verified examples"
    - path: "docs/usage.md"
      provides: "CLI usage guide (pipelines-first) + embedded troubleshooting"
    - path: "docs/_fixtures/usage/inputs/hostname.txt"
      provides: "Deterministic doc-test input fixture"
    - path: "docs/_fixtures/usage/templates/hostname.yaml"
      provides: "Deterministic modern template fixture used in docs"
  key_links:
    - from: "README.md"
      to: "docs/usage.md"
      via: "markdown link"
      pattern: "\\(docs/usage\\.md\\)"
    - from: "docs/usage.md"
      to: "cliscrape parse"
      via: "console examples"
      pattern: "\\$ cliscrape parse"
---

<objective>
Create the user-facing entrypoint (README) and a pipelines-first CLI usage guide, including deterministic examples backed by small fixtures.

Purpose: Make it easy for CLI users to start parsing immediately and find the right deeper documentation.
Output: Updated README + docs/usage.md + minimal fixtures used by docs-as-tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-documentation-authoring-guide/11-CONTEXT.md
@.planning/phases/11-documentation-authoring-guide/11-RESEARCH.md

@README.md
@src/cli.rs
@src/main.rs
@src/logging.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README as the short entry point</name>
  <files>README.md</files>
  <action>
Update `README.md` to be an onboarding-oriented entry point (not a full guide) that matches current shipped behavior and `.planning/ROADMAP.md` (remove the stale roadmap checklist).

Include:
- What cliscrape does in 2-4 sentences (fast CLI parsing into structured output; TextFSM compatible + modern templates; TUI Live Lab).
- A short "Install" section (build from source: `cargo install --path .` and/or `cargo run -- ...`; do NOT claim crates.io availability unless the repo already supports it).
- A short "Quickstart" with 2-3 deterministic `console` blocks that will be validated later:
  - Parse using a local modern template + file input (use fixtures created in Task 2).
  - Show template discovery commands (`list-templates` with `--filter` + `--format json` and `show-template` for a single known embedded template, but keep expected output minimal with trycmd-friendly elisions).
- Links to the deeper docs pages that will exist after this phase:
  - `docs/usage.md`
  - `docs/template-authoring.md`
  - `docs/migration.md`
  - `docs/templates.md`

Constraints:
- Keep examples portable (no shell pipes, no process substitution, no glob expansion) because trycmd runs commands directly.
- Prefer `--format json` for stdout determinism; avoid `-v` unless the point is logging.
- Any command output included in a `console` block should be stable; use `...` / `[..]` elisions where necessary.
  </action>
  <verify>
Run `cargo run -- --help` to ensure examples reference real commands/flags.
  </verify>
  <done>
README is short, current, links to docs/, and includes deterministic console examples.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pipelines-first CLI usage guide with minimal fixtures</name>
  <files>
docs/usage.md
docs/_fixtures/usage/inputs/hostname.txt
docs/_fixtures/usage/templates/hostname.yaml
  </files>
  <action>
Create `docs/usage.md` focused on CLI usage and template discovery:

- Explain template selection:
  - path vs identifier behavior (including ambiguity and using extensions / `--template-format`).
  - embedded templates + XDG user overrides (where to place user templates; include the XDG path expectations as described by the project).
- Explain inputs:
  - file args vs `--input` vs `--input-glob` vs `--stdin`.
- Explain output formats:
  - `--format auto` behavior (table when TTY, JSON when not).
  - `--error-format human|json`.
- Explain warnings/errors at a high level:
  - warnings are normal/actionable (coverage/partial matches) and do not print from the library API.
- Troubleshooting embedded in this guide:
  - "Template not found" and "Ambiguous template identifier" resolution steps.
  - Logging/tracing section: `-v` mapping, `RUST_LOG`, `--log-format text|json`, and that logs go to stderr.

Add deterministic fixtures used by examples:
- `docs/_fixtures/usage/inputs/hostname.txt`: a tiny, stable input with one hostname line.
- `docs/_fixtures/usage/templates/hostname.yaml`: a minimal modern template that extracts hostname from that input.

Include multiple `console` blocks that use those fixtures, e.g.:
- `cliscrape parse --template docs/_fixtures/usage/templates/hostname.yaml docs/_fixtures/usage/inputs/hostname.txt --format json`
- One or two discovery commands (`list-templates` with `--filter` and JSON output; `show-template` for one embedded template) with elisions for large/variable sections.

Keep troubleshooting examples copy/pasteable; avoid OS-specific paths.
  </action>
  <verify>
Run `cargo run -- parse --template docs/_fixtures/usage/templates/hostname.yaml docs/_fixtures/usage/inputs/hostname.txt --format json` and confirm it succeeds.
  </verify>
  <done>
docs/usage.md exists with runnable examples and embeds troubleshooting + logging guidance.
  </done>
</task>

</tasks>

<verification>
- `cargo run -- --help`
- `cargo run -- parse --template docs/_fixtures/usage/templates/hostname.yaml docs/_fixtures/usage/inputs/hostname.txt --format json`
</verification>

<success_criteria>
- README acts as an accurate entry point and routes users to `docs/`.
- `docs/usage.md` explains template discovery and parse usage, and includes actionable troubleshooting guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-authoring-guide/11-01-SUMMARY.md`
</output>

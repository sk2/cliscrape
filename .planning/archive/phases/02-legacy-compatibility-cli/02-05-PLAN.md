---
phase: 02-legacy-compatibility-cli
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.rs
  - tests/cli_parse_help.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "`cliscrape parse` exposes Phase-2 input flags (stdin + explicit inputs + input globs)"
    - "`cliscrape parse` exposes Phase-2 output + error flags (format=auto, error-format=human|json)"
    - "`--template/-t` accepts a template spec string (path or identifier), with a template-format override flag"
  artifacts:
    - path: "src/cli.rs"
      provides: "Clap CLI surface for Phase-2 `parse` contract"
    - path: "tests/cli_parse_help.rs"
      provides: "Help regression test locking Phase-2 flags/defaults"
  key_links:
    - from: "src/cli.rs"
      to: "tests/cli_parse_help.rs"
      via: "help output regression assertions"
---

<objective>
Close verifier gaps by updating the `cliscrape parse` clap surface to match the Phase-2 CLI contract (multi-input flags, `format=auto`, `--error-format`, and `--quiet`) and lock it down with a help regression test.

Purpose: Make Phase-2 CLI behavior implementable in `src/main.rs` without inventing flags or defaults.
Output: Updated clap definitions and a `parse --help` regression test.
</objective>

<execution_context>
@/Users/simonknight/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/simonknight/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-legacy-compatibility-cli/02-CONTEXT.md
@.planning/phases/02-legacy-compatibility-cli/02-RESEARCH.md
@.planning/phases/02-legacy-compatibility-cli/02-VERIFICATION.md
@src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase-2 `parse` flags + defaults to clap</name>
  <files>src/cli.rs</files>
  <action>
    Implement the CLI surface that the verifier reports missing in `src/cli.rs`:
    - Change `--template/-t` from `PathBuf` to a string spec (path OR identifier).
    - Ensure `--template-format <auto|textfsm|yaml|toml>` exists (default `auto`).
      - Accept both `--template-format <fmt>` and `--template-format=<fmt>`.
    - Inputs:
      - Add positional `inputs...` (0+ paths) AND/OR repeatable `--input <PATH>`.
      - Add repeatable `--input-glob <PATTERN>` (explicit expansion in app code later).
      - Add `--stdin` boolean to include stdin in addition to file inputs.
    - Output:
      - Add `OutputFormat::Auto` and set `--format/-f` default to `auto`.
    - Errors:
      - Add global root flag `--error-format <human|json>` default `human`.
    - Status:
      - Add `--quiet` to `parse` to suppress the success status line (warnings still print).

    Keep `debug` and `convert` compiling; only adjust their structs if the root flag addition requires it.
  </action>
  <verify>`cargo test -q` passes.</verify>
  <done>`cargo run -- parse --help` shows `--stdin`, `--input`, `--input-glob`, `--format auto` default, `--error-format human` default, and `--quiet`.</done>
</task>

<task type="auto">
  <name>Task 2: Add `parse --help` regression test for the Phase-2 contract</name>
  <files>tests/cli_parse_help.rs</files>
  <action>
    Create `tests/cli_parse_help.rs` using `assert_cmd` that runs `cliscrape parse --help` and asserts the help output includes:
    - `--template` (string spec), `--template-format` with `auto|textfsm|yaml|toml`
    - input flags: positional `inputs`, `--input`, `--input-glob`, `--stdin`
    - output flags: `--format` includes `auto|json|csv|table` with default `auto`
    - error flags: root `--error-format` includes `human|json` with default `human`
    - status flag: `--quiet`
  </action>
  <verify>`cargo test -q` passes.</verify>
  <done>The test fails if any Phase-2 required flag or default is removed/changed.</done>
</task>

</tasks>

<verification>
1. `cargo test -q` passes.
2. `cargo run -- parse --help` documents the full Phase-2 CLI contract for parse.
</verification>

<success_criteria>
- The verifier gaps for Phase-2 CLI flags/defaults are closed (flags exist and defaults match decisions).
- The contract is enforced by an automated help regression test.
</success_criteria>

<output>
After completion, create `.planning/phases/02-legacy-compatibility-cli/02-05-SUMMARY.md`
</output>

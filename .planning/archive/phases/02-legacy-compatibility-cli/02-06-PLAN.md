---
phase: 02-legacy-compatibility-cli
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.rs
  - src/engine/records.rs
  - src/engine/fsm.rs
  - src/template/loader.rs
  - src/template/textfsm.pest
  - tests/textfsm_compat.rs
  - tests/fixtures/textfsm/error_action.textfsm
  - tests/fixtures/textfsm/clear_vs_clearall.textfsm
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "`Clear` vs `Clearall` semantics match TextFSM/ntc-templates expectations"
    - "`-> Error` aborts parsing and discards all rows (fail-fast, no partial output)"
  artifacts:
    - path: "src/engine/types.rs"
      provides: "Action model supports ClearAll and Error"
    - path: "src/template/loader.rs"
      provides: "TextFSM loader parses Clear/Clearall/Error distinctly"
    - path: "tests/textfsm_compat.rs"
      provides: "Fixture-backed regression tests for action semantics"
  key_links:
    - from: "src/template/loader.rs"
      to: "src/engine/fsm.rs"
      via: "TemplateIR action lowering -> runtime execution"
---

<objective>
Close TextFSM action-semantics gaps by implementing distinct `Clear` vs `Clearall` behavior and supporting `-> Error` as a fail-fast action, with fixture-backed regression tests.

Purpose: Align runtime behavior with ntc-templates expectations so legacy templates are safe and predictable.
Output: Engine/loader/grammar updates and action regression fixtures + tests.
</objective>

<execution_context>
@/Users/simonknight/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/simonknight/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-legacy-compatibility-cli/02-CONTEXT.md
@.planning/phases/02-legacy-compatibility-cli/02-RESEARCH.md
@.planning/phases/02-legacy-compatibility-cli/02-VERIFICATION.md
@src/template/loader.rs
@src/engine/fsm.rs
@src/engine/records.rs
@src/template/textfsm.pest
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ClearAll + Error actions end-to-end (grammar -> loader -> runtime)</name>
  <files>src/engine/types.rs, src/engine/records.rs, src/engine/fsm.rs, src/template/loader.rs, src/template/textfsm.pest</files>
  <action>
    Implement the missing TextFSM actions called out in `.planning/phases/02-legacy-compatibility-cli/02-VERIFICATION.md`:
    - Extend `src/engine/types.rs` to represent `ClearAll` and `Error` distinctly.
    - Update `src/template/textfsm.pest` so record actions include `Error` and `Clearall` tokens.
    - Update `src/template/loader.rs` to map:
      - `Clear` -> `Action::Clear`
      - `Clearall` -> `Action::ClearAll`
      - `Error` -> `Action::Error`
    - Update `src/engine/records.rs` so:
      - `Clear` clears only non-Filldown values
      - `ClearAll` clears everything (including Filldown)
    - Update `src/engine/fsm.rs` so `Action::Error` aborts parsing with an error (fail-fast).

    Keep existing behavior for templates that do not use these actions.
  </action>
  <verify>`cargo test -q` passes.</verify>
  <done>Templates using `-> Clearall` preserve TextFSM behavior (Filldown cleared), and `-> Error` stops parsing with an error.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture-backed regression tests for Clear vs Clearall and Error</name>
  <files>tests/textfsm_compat.rs, tests/fixtures/textfsm/error_action.textfsm, tests/fixtures/textfsm/clear_vs_clearall.textfsm</files>
  <action>
    Add two fixtures and corresponding tests in `tests/textfsm_compat.rs`:
    - `tests/fixtures/textfsm/error_action.textfsm`:
      - Contains a rule that triggers `-> Error` on a recognizable line.
      - Test asserts parsing returns `Err(...)` and no rows are produced.
    - `tests/fixtures/textfsm/clear_vs_clearall.textfsm`:
      - Uses a `Value Filldown` and a non-filldown value.
      - Triggers `-> Clear` and verifies Filldown persists across subsequent `Record`.
      - Triggers `-> Clearall` and verifies Filldown does not persist.

    Ensure tests are deterministic and do not rely on stdout/stderr (pure library parse tests).
  </action>
  <verify>`cargo test -q` passes and the new tests fail against the pre-gap implementation.</verify>
  <done>Regression tests prove Clear preserves Filldown, Clearall clears Filldown, and Error aborts parsing.</done>
</task>

</tasks>

<verification>
1. `cargo test -q` passes.
2. `tests/textfsm_compat.rs` contains explicit assertions for Clear/Clearall and Error semantics.
</verification>

<success_criteria>
- Verifier gaps for TextFSM actions (Clear vs Clearall, Error) are closed.
- Behavior is locked down via fixtures so future changes regress loudly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-legacy-compatibility-cli/02-06-SUMMARY.md`
</output>

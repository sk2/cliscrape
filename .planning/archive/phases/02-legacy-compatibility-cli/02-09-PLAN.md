---
phase: 02-legacy-compatibility-cli
plan: 09
type: execute
wave: 4
depends_on: ["02-06", "02-08"]
files_modified:
  - templates/modern/simple_hostname.toml
  - tests/cli_parse_e2e.rs
  - tests/fixtures/inputs/hostname_file.txt
  - tests/fixtures/inputs/hostname_stdin.txt
  - tests/fixtures/inputs/textfsm_required_ok.txt
  - tests/fixtures/inputs/textfsm_error_ok.txt
  - tests/fixtures/inputs/textfsm_error_trigger.txt
  - tests/fixtures/textfsm/test_required.textfsm
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Phase-2 regression/e2e tests and fixtures exist for CLI parse contract"
    - "Multi-input ordering is stable (files first, stdin last) and test-covered"
    - "Template identifier resolution is deterministic and test-covered"
    - "TextFSM `Required` + `Filldown` interaction is fixture-backed and e2e-covered"
  artifacts:
    - path: "tests/cli_parse_e2e.rs"
      provides: "Fixture-backed CLI e2e tests for parse"
    - path: "tests/fixtures/inputs/hostname_stdin.txt"
      provides: "Stdin fixture input"
    - path: "tests/fixtures/textfsm/test_required.textfsm"
      provides: "TextFSM fixture template for required gating"
  key_links:
    - from: "tests/cli_parse_e2e.rs"
      to: "src/main.rs"
      via: "assert_cmd executing `cliscrape parse`"
---

<objective>
Close the remaining verification gap for Phase-2 by adding fixture-backed CLI e2e tests that lock down: file parsing, piped stdin parsing, stdin+file combination ordering, TextFSM parsing via explicit path, and template identifier resolution via cwd-only search.

Purpose: Prevent regressions in the Unix-style CLI contract and the template resolution rules.
Output: `tests/cli_parse_e2e.rs` plus fixtures and a tiny modern template used by the tests.
</objective>

<execution_context>
@/Users/simonknight/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/simonknight/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-legacy-compatibility-cli/02-VERIFICATION.md
@src/main.rs
@src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI e2e fixtures (modern + TextFSM + inputs)</name>
  <files>templates/modern/simple_hostname.toml, tests/fixtures/inputs/hostname_file.txt, tests/fixtures/inputs/hostname_stdin.txt, tests/fixtures/inputs/textfsm_required_ok.txt, tests/fixtures/textfsm/test_required.textfsm, tests/fixtures/inputs/textfsm_error_ok.txt, tests/fixtures/inputs/textfsm_error_trigger.txt</files>
  <action>
    Create fixtures used by e2e tests:
    - `templates/modern/simple_hostname.toml`: a minimal modern template that captures `hostname` from `Hostname: ...`.
    - `tests/fixtures/inputs/hostname_file.txt`: contains `Hostname: FileHost`.
    - `tests/fixtures/inputs/hostname_stdin.txt`: contains `Hostname: StdinHost`.
    - `tests/fixtures/textfsm/test_required.textfsm`: minimal TextFSM template that uses `Value Required` + `Filldown` together (e.g., a Filldown+Required value that is omitted on later records but still satisfies Required via Filldown).
    - `tests/fixtures/inputs/textfsm_required_ok.txt`: input that satisfies the required+filldown template and produces 2+ rows (at least one row where the Required field is satisfied via Filldown).
    - `tests/fixtures/inputs/textfsm_error_ok.txt`: input that produces at least one row under `tests/fixtures/textfsm/error_action.textfsm` (fixture created in plan 02-06) without triggering `-> Error`.
    - `tests/fixtures/inputs/textfsm_error_trigger.txt`: input that triggers `-> Error` under `tests/fixtures/textfsm/error_action.textfsm` (fixture created in plan 02-06).
  </action>
  <verify>`cargo test -q` passes (may still be missing the e2e test file until Task 2).</verify>
  <done>All fixtures exist at the specified paths and are usable from tests without relying on `examples/`.</done>
</task>

<task type="auto">
  <name>Task 2: Add `cliscrape parse` e2e tests for file, stdin, combined ordering, TextFSM, and identifier resolution</name>
  <files>tests/cli_parse_e2e.rs</files>
  <action>
    Create `tests/cli_parse_e2e.rs` using `assert_cmd` that runs the built binary and asserts:
    1. File input -> JSON stdout with `hostname == "FileHost"`.
    2. Piped stdin -> JSON stdout with `hostname == "StdinHost"`.
    3. Stdin + file with `--stdin` -> JSON array order follows Phase-2 rule: file record first, stdin record last.
    4. TextFSM template by explicit path (Required+Filldown):
       - Run with `-t tests/fixtures/textfsm/test_required.textfsm` and `tests/fixtures/inputs/textfsm_required_ok.txt`.
       - Assert JSON has 2+ rows and at least one later row includes the Filldown+Required value populated via Filldown.
    5. Identifier resolution via cwd-only search:
       - Run command with `current_dir("tests/fixtures/textfsm")`.
       - Use `-t test_required` and an input path that exists from that cwd.
       - Assert results match the explicit-path Required+Filldown test.
    6. Negative: no partial stdout on later failure:
       - Run with `-t tests/fixtures/textfsm/error_action.textfsm` and two inputs: `tests/fixtures/inputs/textfsm_error_ok.txt` then `tests/fixtures/inputs/textfsm_error_trigger.txt`.
       - Assert exit code `1`.
       - Assert stdout is empty (or parses as empty) to prove no partial JSON is emitted before the later failure.
  </action>
  <verify>`cargo test -q` passes and the e2e tests fail against pre-gap behavior.</verify>
  <done>Phase-2 CLI e2e coverage exists and detects regressions in stdin, multi-input ordering, TextFSM parsing, and identifier resolution.</done>
</task>

</tasks>

<verification>
1. `cargo test -q` passes.
2. `tests/cli_parse_e2e.rs` covers: file input JSON, piped stdin JSON, stdin+file ordering determinism, TextFSM parsing, and identifier-based template resolution.
</verification>

<success_criteria>
- Verifier gap "Planned Phase-2 regression/e2e tests and fixtures" is closed.
- Key CLI behaviors are enforced by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-legacy-compatibility-cli/02-09-SUMMARY.md`
</output>

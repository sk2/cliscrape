---
phase: 02-legacy-compatibility-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.rs
  - tests/cli_parse_help.rs
autonomous: true

must_haves:
  truths:
    - "`cliscrape parse` exposes Phase-2 input flags (stdin + explicit inputs + input globs)"
    - "`cliscrape parse` exposes Phase-2 output + error flags (format=auto, error-format=human|json)"
    - "Template selection accepts either a path or an identifier/name (string), with format override"
  artifacts:
    - path: "src/cli.rs"
      provides: "Clap CLI surface matching Phase 2 decisions"
    - path: "tests/cli_parse_help.rs"
      provides: "CLI help regression coverage for new flags"
  key_links:
    - from: "src/main.rs"
      to: "src/cli.rs"
      via: "Cli::try_parse + Parse args"
---

<objective>
Update the `cliscrape parse` CLI surface so it matches the Phase 2 locked UX decisions (TTY-aware output defaults, multi-input support, and structured error mode) without changing engine semantics yet.

Purpose: Make the CLI contract explicit and testable so Phase 2 behavior can be implemented in `src/main.rs` with minimal ambiguity.
Output: Updated clap definitions and a help-regression test.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-legacy-compatibility-cli/02-CONTEXT.md
@.planning/phases/02-legacy-compatibility-cli/02-RESEARCH.md
@src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update `cliscrape parse` clap flags to Phase 2 contract</name>
  <files>src/cli.rs</files>
  <action>
    Update `src/cli.rs` to reflect the Phase 2 locked decisions (do not implement behavior here; just define the CLI):
    1. `parse` subcommand
       - `--template/-t <TEMPLATE>`: accept a *string* spec (path or identifier/name). Keep it required.
       - `--template-format <auto|textfsm|yaml|toml>`: keep existing enum, default `auto`.
       - Inputs:
          - positional `inputs...` (0+ file paths) for explicit input files.
          - `--input <PATH>` repeatable (aliases positional; executor may consolidate later).
          - `--input-glob <PATTERN>` repeatable for app-level glob expansion (must not be implicit).
          - `--stdin` boolean to include stdin *in addition to* any file inputs.
       - Output:
          - `--format/-f <auto|json|csv|table>` with default `auto` (Phase 2: TTY -> table, non-TTY -> json).
       - Errors:
          - Add global `--error-format <human|json>` on the root `Cli` so clap argument errors can also be formatted (default `human`).
       - Status:
          - Add `--quiet` on `parse` to suppress the success status line (warnings still print to stderr).
    2. Preserve existing `debug` and `convert` subcommands; only adjust them if necessary to keep compilation.
  </action>
  <verify>`cargo test -q` passes (including new help test from Task 2).</verify>
  <done>`cargo run -- parse --help` shows the new flags and defaults (format=auto, template-format=auto, error-format=human) and documents `--quiet`.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI help regression test for parse contract</name>
  <files>tests/cli_parse_help.rs</files>
  <action>
    Add `tests/cli_parse_help.rs` using `assert_cmd` that:
    1. Runs `cliscrape parse --help`.
    2. Asserts the output mentions:
       - `--template` accepts a value (string spec)
       - `--template-format` with `auto|textfsm|yaml|toml`
       - `--input-glob` and `--stdin`
       - `--format` with `auto|json|csv|table` and default `auto`
       - `--error-format` with `human|json` and default `human`
       - `--quiet`
  </action>
  <verify>`cargo test -q` passes.</verify>
  <done>Help output test fails if any Phase-2-required flag is removed or default changes.</done>
</task>

</tasks>

<verification>
1. `cargo test -q` passes.
2. `cargo run -- parse --help` documents all Phase 2 CLI contract flags.
</verification>

<success_criteria>
- CLI surface area matches Phase 2 decisions (flags exist; defaults set correctly).
- Executor can implement Phase 2 behavior in `src/main.rs` without inventing missing flags.
</success_criteria>

<output>
After completion, create `.planning/phases/02-legacy-compatibility-cli/02-01-SUMMARY.md`
</output>
